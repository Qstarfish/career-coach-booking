<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careercoach.careercoachbooking.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.careercoach.careercoachbooking.entity.User">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, name, email, created_at, updated_at
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT id, user_id, name, email, created_at, updated_at
        FROM user
        WHERE email = #{email}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (user_id, name, email)
        VALUES (#{userId}, #{name}, #{email})
    </insert>

    <update id="update">
        UPDATE user
        SET name = #{name},
            email = #{email}
        WHERE user_id = #{userId}
    </update>

</mapper>

